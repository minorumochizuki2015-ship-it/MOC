{
  "version": "0.2.0",
  "configurations": [
    {
      "envFile": "${workspaceFolder}\\.env",
      "justMyCode": true,
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "type": "python",
      "name": "Python: Current File",
      "program": "${file}"
    }
  ]
}