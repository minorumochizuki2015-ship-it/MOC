---
description: Protect start_modern_ui.bat & main_modern.py. Plan→Approval→PATCH only.
globs:
  - "start_modern_ui.bat"
  - "main_modern.py"
alwaysApply: true
---
【PROTECTED】対象は `start_modern_ui.bat` と `main_modern.py` のみ。変更は Plan→承認→PATCH。
Dry-Run既定、Apply時のみ書込。バックアップ→原子的書込(temp→rename)→直後検証(/v1/models, tkinter)。
失敗時は即ロールバック。エンコ/EOL/不要行の変更禁止。